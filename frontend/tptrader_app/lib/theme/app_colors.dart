import 'package:flutter/material.dart';

class AppColors {
  // Background gradients
  static const Color backgroundGradientStart = Color(0xFF5B2C83);
  static const Color backgroundGradientEnd   = Color(0xFF8E44AD);

  // Cards
  static const Color cardWhite = Colors.white;              // used by AppCard
  static const Color cardBackground = Colors.white;

  // Inputs
  static const Color inputFill = Color(0xFFF6F2FF);         // used by InputBox
  static const Color inputBackground = Color(0xFFF6F2FF);

  // Buttons + brand colors
  static const Color primary = Color(0xFF7D3C98);           // used by PrimaryButton
  static const Color primaryPurple = Color(0xFF7D3C98);
  static const Color primaryPurpleDark = Color(0xFF5B2C83);

  // Text colors
  static const Color textPrimary = Color(0xFF1F1235);
  static const Color textSecondary = Color(0xFF5C5C6E);
  static const Color textGrey = Color(0xFF9B9B9B);

  // Borders & shadows
  static const Color borderLight = Color(0xFFE0D4FF);
  static const Color shadowSoft = Color(0x22000000);

  static const Color textDark = Color(0xff4A148C);
}
